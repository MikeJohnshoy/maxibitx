# TX Power Calibration: Getting Flat Power Across Bands

Status: **done** (2026-09) - all 9 `[tx_band]` entries in
`data/hw_settings.ini` are now bench-confirmed in the 4.7-5.5W target
window at `TX_DRIVE`=50; see §5's worksheet for the final numbers. The bench procedure below - one popular QRP
CW frequency per band, into a dummy load - is what produced the final
values; §7 is still worth reading if a future board revision needs this
re-run and a band won't reach target.

## 1. Background

`hw_settings.ini` carries a `[tx_band]` section per HF band, each with
an `f_start`/`f_stop` range and a `scale` value. minibitx's CW TX audio is also generated by a 
different pipeline than sbitx's own FFT-domain modulator (see
[`../03_tx_processing_pipeline.md`](../03_tx_processing_pipeline.md)),
so reusing sbitx's `scale` *numbers* unchanged was never expected to
carry over exactly - only the relative *shape* across bands (real PA
rolloff data) is trustworthy as a starting point.

## 2. What changed since the last calibration attempt

I moved the CW TX carrier from a bare 700Hz tone straight into
the balanced modulator (sitting deep inside the crystal filter's
passband on both mixing products - genuine DSB) to a carrier shifted up
by `TX_IF_OFFSET_HZ` (~23.3kHz), deliberately landing the wanted product
at the crystal filter's real measured center - its point of *least*
attenuation, with the unwanted image out on its skirt (full mechanism in
[`../03_tx_processing_pipeline.md`](../03_tx_processing_pipeline.md)'s
"Mixer 2" section). 

## 3. Test frequencies - one popular QRP CW frequency per band

Rather than an arbitrary band-midpoint, calibrating at a frequency
someone might actually operate CW QRP on doubles this session as a
real-world checkpoint. These are the long-standing, widely-used QRP CW
calling frequencies - not something minibitx enforces or checks against
any band plan, so confirm they fall inside your own license class and
region's allocation before ever keying into an antenna rather than a
dummy load:

| Band | `hw_settings.ini` range | Test frequency | Note |
|---|---|---|---|
| 80m | 3.5-4.0 MHz | 3.560 MHz | traditional QRP CW calling frequency |
| 60m | 5.25-5.45 MHz | ~5.357 MHz | 60m has no traditional QRP calling freq (channelized in the US) - picked mid-band as a representative bench point |
| 40m | 7.0-7.3 MHz | 7.030 MHz | traditional QRP CW calling frequency|
| 30m | 10.0-10.2 MHz | 10.106 MHz | traditional QRP CW calling frequency |
| 20m | 14.0-14.35 MHz | 14.060 MHz | traditional QRP CW calling frequency |
| 17m | 18.0-18.2 MHz | 18.086 MHz | traditional QRP CW calling frequency |
| 15m | 21.0-21.45 MHz | 21.060 MHz | traditional QRP CW calling frequency |
| 12m | 24.8-25.0 MHz | 24.906 MHz | traditional QRP CW calling frequency |
| 10m | 28.0-29.7 MHz | 28.060 MHz | traditional QRP CW calling frequency |

## 4. Calibration procedure

No rebuild is needed between trials - `hw_settings.ini` is read once at
startup (`hw_settings_load()`, `hw_settings.c`), so each trial is just:
edit the file, restart minibitx, key down, read the meter. Into a dummy
load throughout, `TX_DRIVE` fixed at 50 (see §6 for the live-drive-control
idea this is a placeholder for).

For each of the 9 `[tx_band]` entries:

1. Tune to that band's test frequency (§3 table).
2. Key down, hold a couple of seconds, and read the **steady** wattmeter
   value (not the initial turn-on kick - see the attack-transient note
   in `sound.c`'s comments).
3. Bisect that band's `scale` value in `hw_settings.ini` up or down
   until the steady reading is ~5.0W. Record the final `scale`.
4. Move to the next band.

Since every band now has real headroom below the clamp (§2), this
should be a straightforward bisection on every band - unlike the
previous session, there's no expectation any band is ceiling-limited
going in. §7 covers what to do if one turns out to be anyway.

## 5. Worksheet

The 40m row is already filled in from the bench session that landed on
`TX_GAIN_CORRECTION`=0.045 - it happened to test at 7.020 MHz rather
than the 7.030 QRP convention above, and landed close enough to target
(5.1W) that it's reasonable to treat as done rather than re-run, unless
you want the exact QRP frequency on record too.

First bench pass (2026-09) took one measurement per band at the
starting `scale` from real sbitx's table, then used a one-step
prediction (`tuned_scale = starting_scale * sqrt(5W / measured_W)`,
since `sound.c`'s TX amplitude is directly proportional to `scale` and
power into a resistive dummy load scales as amplitude squared for a
linear, non-saturated chain - true here per §2) instead of a full
manual bisection.

That predicted step under- or over-shot on several bands on the first
re-test (80m and 60m landed low; 17m and 12m appeared to move the
*wrong* direction entirely). Re-testing 17m and 12m again with **no**
scale change resolved the apparent wrong-direction anomaly - both
landed in-range on a second look, meaning it was session-to-session
measurement variability, not a bad `scale` value. Across this whole
calibration pass, band-to-band/session-to-session readings varied by
roughly +/-0.5-0.8W (plausibly PA warm-up, supply voltage, or dummy
load drift over the course of a bench session) - noticeably larger than
the +/-0.1W jitter seen *within* a single key-down. 80m needed one
further manual step beyond the predicted value (down to 0.000850) to
land in range; the acceptance target throughout was **4.7-5.5W**, not
an exact 5.0W, specifically to stay clear of that noise floor.

| Band | Test freq | Final `scale` | Confirmed reading | Status |
|---|---|---|---|---|
| 80m | 3.560 MHz | 0.000850 | 5.5W | confirmed |
| 60m | ~5.357 MHz | 0.00109 | 5.5W | confirmed |
| 40m | 7.030 MHz | 0.00112 | 5.6W | confirmed  |
| 30m | 10.106 MHz | 0.00174 | 4.8W | confirmed |
| 20m | 14.060 MHz | 0.00314 | 5.6W | confirmed |
| 17m | 18.086 MHz | 0.00384 | 5.5W | confirmed |
| 15m | 21.060 MHz | 0.00500 | 5.5W | confirmed |
| 12m | 24.906 MHz | 0.00709 | 5.3W | confirmed |
| 10m | 28.060 MHz | 0.01011 | 5.6W | confirmed |

`data/hw_settings.ini` carries these final values, each with a
bench-confirmed comment. All 9 bands are now confirmed in the
4.7-5.5W target window - this calibration pass is done.

## 6. Future: a live 'drive' control

`TX_DRIVE` is a fixed compile-time constant (50) today - there's no live
UI or command to change it. The intent is that a future control would
let an operator dial drive above this bench-calibrated 5W-per-band
baseline, trading the "flat, safe, QRP-appropriate" default calibrated
here for more available power at their own discretion and risk (up to
whatever the PA and this bench session's headroom can actually support -
see §7 for where that runs out). Not implemented yet; recording the
intent here so the 5W target above is understood as a deliberate,
conservative default rather than a hard ceiling nobody thought to make
adjustable.

## 7. If a band can't reach 5W

If a band turns out to be genuinely ceiling-limited even after bisecting
`scale` all the way up toward the clamp, the options are, roughly in
order of effort:

- **Accept it and document the real number.** A slightly-under-5W CW
  signal on the hardest band is still a working signal; this may be
  perfectly fine for minibitx's scope as a support layer. Worth checking
  against real sbitx's own spec for this board family, which claims 40W
  on 80m/40m but only "6 watts on 10M" - if even the reference design's
  PA rolls off that hard up there, a from-scratch 5W target on 10m/12m
  may be optimistic regardless of `scale`.
- **Check for a little spare clamp headroom.** `TX_SAMPLE_CLAMP` (2×10⁹)
  is set with some margin below the true ±2.147×10⁹ a 32-bit sample
  allows. Raising the clamp closer to that limit buys a small (~7%),
  fixed amount of extra headroom - worth trying, but it's a small knob,
  not a fix for a large shortfall.
- **Look for headroom outside the digital pipeline entirely** - e.g.
  whether there's any other real analog gain stage available on this
  board. This is genuinely new investigation, not a tuning exercise,
  and shouldn't be assumed to exist without checking - PA gain rolloff
  across HF is a real hardware property, not something software can
  manufacture out of nothing.

Whatever the outcome, keep the measured-vs-target gap documented here
rather than silently accepted - a future board revision or PA change is
the kind of thing that would make it worth re-running this procedure
from scratch.
